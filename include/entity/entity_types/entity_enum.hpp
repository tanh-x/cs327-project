#ifndef PROJECT_ENTITY_ENUM_HPP
#define PROJECT_ENTITY_ENUM_HPP

#include "memory"

enum class EntityEnum {
    PLAYER,
    HIKER,
    RIVAL,
    SWIMMER,
    PACER,
    WANDERER,
    SENTRY,
    EXPLORER
};

#endif
